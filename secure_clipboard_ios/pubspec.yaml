name: secure_clipboard_ios
version: 0.0.1

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=3.0.0"

flutter:
  plugin:
    implements: secure_clipboard
    platforms:
      ios:
        pluginClass: SecureClipboardIosPlugin

dependencies:
  flutter:
    sdk: flutter
  secure_clipboard_platform_interface:
    path: ../secure_clipboard_platform_interface

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
